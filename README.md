# sql-dwh-etl
Building a modern data warehouse with SQL Server using the Medallion Architecture (Bronze → Silver → Gold). The project features automated ETL pipelines that ingest, clean, standardize, and transform CRM/ERP data into reliable, business-ready analytical models.
